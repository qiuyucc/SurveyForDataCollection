@model chensFyp.Models.Reward

<h3>Are you sure you want to redeem this?</h3>
<div>
    <h4>Reward</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.RewardName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.RewardName)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.point)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.point)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Redeem" class="btn btn-default" /> |
            @Html.ActionLink("Back to List", "Index_Memeber")
        </div>
    }
</div>
